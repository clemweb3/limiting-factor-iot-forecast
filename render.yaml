services:
  - type: web
    name: cozysense-api
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PROACTIVE_API_KEY
        generateValue: true
